# `.github/workflows`

GitHub-Actions-Workflows.

- `ci.yml`: Fuehrt auf Pull Requests und Pushes nach `main` die wichtigsten Qualitaetspruefungen aus.

Die CI ist bewusst kompakt: Python-Abhaengigkeiten, Node-Abhaengigkeiten, Django-Systemcheck, Pytest und Playwright-Browsertests.
