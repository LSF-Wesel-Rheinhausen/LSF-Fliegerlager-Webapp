# Add meal booking from calendar days

- Adds a direct booking action to open meal-calendar days in the kiosk.
- Preselects the chosen day and opens the existing participant selection step.
- Adds server-rendering and browser regression coverage for the direct flow.
