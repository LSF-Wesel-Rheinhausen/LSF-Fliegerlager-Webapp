# Offizielle Coverage-Messung

## Zusammenfassung
- `pytest-cov` wurde in den Entwicklungs-Abhängigkeiten ergänzt.
- Konfiguration für `pytest-cov` wurde in `pyproject.toml` ergänzt, um Tests auf `src/billing` zu fokussieren.
- Zielkommando für die Test-Abdeckung wurde in der `README.md` dokumentiert.

Dies schließt den ersten Schritt in der `TODO.md` ab.
