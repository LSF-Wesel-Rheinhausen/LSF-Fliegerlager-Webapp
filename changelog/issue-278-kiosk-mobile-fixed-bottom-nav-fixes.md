# Kiosk Mobile & Layout Fixes

- Fixed mobile navigation behavior (top menu correctly hidden, bottom menu correctly shown on mobile but hidden on desktop).
- Positioned Darkmode toggle perfectly in the brand header.
- Aligned date input formats and validation strictly to `DD.MM.YYYY` in Django form constraints, templates, and Playwright E2E tests.
- Bumped PWA cache version to force client updates.
